import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  mainContainer: {
    minWidth: "100%",
    minHeight: "100vh",
    backgroundColor: "white",
    marginTop: "4rem",
  },
  title: {
    padding: "24px 0",
    color: "#08F387",
  },
}));
